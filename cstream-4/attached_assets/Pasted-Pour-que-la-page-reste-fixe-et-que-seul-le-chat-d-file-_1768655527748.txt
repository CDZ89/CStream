Pour que **la page reste fixe** et que **seul le chat défile vers le bas**, tu dois structurer ton layout avec du CSS bien pensé. Voici exactement comment faire :

---

## ✅ **1. Structure HTML/JSX recommandée**

```tsx
<div className="page-container">
  <div className="header">CStream</div>

  <div className="chat-container">
    {/* Messages */}
    <div className="chat-messages">
      {/* ...messages ici... */}
    </div>

    {/* Input */}
    <div className="chat-input">
      {/* ...champ de saisie ici... */}
    </div>
  </div>
</div>
```

---

## 🎨 **2. CSS pour fixer la page et scroller uniquement le chat**

```css
.page-container {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #0a0a0f;
  color: white;
  overflow: hidden;
}

.header {
  padding: 16px;
  font-size: 24px;
  font-weight: bold;
  background: #111;
  border-bottom: 1px solid #222;
}

.chat-container {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.chat-messages {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.chat-input {
  padding: 12px 16px;
  border-top: 1px solid #222;
  background: #111;
}
```

---

## 🧠 Explication

- `page-container` prend **toute la hauteur de l’écran** (`100vh`)
- `chat-container` est en `flex: 1` → il occupe tout l’espace restant sous le header
- `chat-messages` est en `overflow-y: auto` → **c’est lui qui scrolle**
- `chat-input` reste **fixe en bas**

---

## 🧪 Bonus : scroll automatique vers le bas

Dans React, ajoute ça pour que le chat descende automatiquement :

```tsx
useEffect(() => {
  const container = document.querySelector(".chat-messages");
  if (container) container.scrollTop = container.scrollHeight;
}, [messages]); // messages = ton tableau de messages
```

---

Tu veux que je te génère un composant React complet avec ce layout ? Ou que je l’adapte à ton design CStream ?