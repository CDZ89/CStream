<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Icône Nuage + Instruction</title>
  <style>
    :root {
      --bg: #0b0b0f;
      --text: #ffffff;
      --muted: #c9c9c9;
      --card: #14141a;
      --border: #2a2a2f;
      --accent: #8b5cf6;
      --accent-light: #a78bfa;
    }
    body {
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Helvetica Neue";
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 20px;
    }
    .notice {
      display: flex;
      align-items: center;
      gap: 18px;
      padding: 24px;
      border: 1px solid var(--border);
      border-radius: 18px;
      background: linear-gradient(145deg, var(--card), #1c1c22);
      box-shadow: 0 12px 28px rgba(0,0,0,.45), inset 0 1px rgba(255,255,255,.08);
      max-width: 640px;
      width: 100%;
      transition: transform .25s ease, box-shadow .25s ease;
    }
    .notice:hover {
      transform: translateY(-2px);
      box-shadow: 0 16px 32px rgba(0,0,0,.55);
    }
    .notice svg {
      width: 72px;
      height: 72px;
      flex-shrink: 0;
      display: block;
      filter: drop-shadow(0 0 6px var(--accent-light));
      animation: pulse 3s infinite;
    }
    @keyframes pulse {
      0% { filter: drop-shadow(0 0 6px var(--accent-light)); }
      50% { filter: drop-shadow(0 0 14px var(--accent)); }
      100% { filter: drop-shadow(0 0 6px var(--accent-light)); }
    }
    .text {
      display: flex;
      flex-direction: column;
      gap: 10px;
      flex: 1;
    }
    .text .title {
      font-weight: 700;
      font-size: 20px;
      letter-spacing: 0.3px;
      color: var(--accent);
    }
    .text .hint {
      color: var(--muted);
      font-size: 16px;
      line-height: 1.6;
    }

    /* Responsive */
    @media (max-width: 600px) {
      .notice {
        flex-direction: column;
        text-align: center;
        gap: 14px;
        padding: 20px;
      }
      .notice svg {
        width: 56px;
        height: 56px;
      }
      .text .title {
        font-size: 18px;
      }
      .text .hint {
        font-size: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="notice" role="note" aria-label="Conseil de lecture">
    <!-- Icône nuage animé -->
    <svg viewBox="0 0 64 64" aria-hidden="true">
      <g fill="none" stroke="#ffffff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M22 40h20a10 10 0 0 0 0-20h-1.2a12 12 0 0 0-22.6 3.5A9 9 0 0 0 22 40z"/>
      </g>
    </svg>

    <div class="text">
      <div class="title">Changer de langue</div>
      <div class="hint">Dans le lecteur ci‑dessous, pour changer de langue, cliquez dessus&nbsp;!</div>
    </div>
  </div>
</body>
</html>
