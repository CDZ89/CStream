Tu veux recréer **l’overlay “Now Playing” de Spotify** — celui qui apparaît quand une musique est lancée, avec la **boucle animée**, le **temps écoulé**, la **barre de progression**, et parfois une **animation visuelle** autour de la pochette.

Tu peux totalement faire ça en HTML/CSS/JS, et c’est même assez fun à construire.  
Je te propose une explication claire + un exemple minimal que tu peux intégrer dans ton app (comme ton projet CStream).

---

# 🎧 **Comment reproduire un overlay façon Spotify ?**

## 🟩 1. Structure générale
Tu as besoin de :
- une **zone overlay** (position: fixed)
- la **pochette**
- le **titre / artiste**
- une **barre de progression animée**
- un **timer** (00:00 / durée)
- éventuellement une **animation de fond** (blur, gradient, etc.)

---

# 🟦 2. Exemple simple (HTML/CSS/JS)

Tu peux coller ça dans ton projet pour tester :

```html
<div class="player-overlay">
  <img src="cover.jpg" class="cover" />

  <div class="info">
    <h2>Titre de la musique</h2>
    <p>Artiste</p>

    <div class="progress-container">
      <span id="current">0:00</span>
      <div class="progress-bar">
        <div id="progress"></div>
      </div>
      <span id="duration">3:42</span>
    </div>
  </div>
</div>
```

### 🎨 CSS (style Spotify-like)

```css
.player-overlay {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20px;
  background: rgba(20, 20, 20, 0.85);
  backdrop-filter: blur(10px);
  display: flex;
  align-items: center;
  gap: 20px;
  color: white;
  animation: fadeIn 0.4s ease;
}

.cover {
  width: 70px;
  height: 70px;
  border-radius: 8px;
  object-fit: cover;
}

.progress-container {
  display: flex;
  align-items: center;
  gap: 10px;
  width: 300px;
}

.progress-bar {
  flex: 1;
  height: 4px;
  background: #555;
  border-radius: 4px;
  overflow: hidden;
}

#progress {
  height: 100%;
  width: 0%;
  background: #1db954;
  transition: width 0.2s linear;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
```

---

# 🟧 3. JS pour animer le temps + la barre

```js
const audio = new Audio("music.mp3");

audio.addEventListener("loadedmetadata", () => {
  document.getElementById("duration").textContent =
    formatTime(audio.duration);
});

audio.addEventListener("timeupdate", () => {
  document.getElementById("current").textContent =
    formatTime(audio.currentTime);

  const percent = (audio.currentTime / audio.duration) * 100;
  document.getElementById("progress").style.width = percent + "%";
});

function formatTime(sec) {
  const m = Math.floor(sec / 60);
  const s = Math.floor(sec % 60).toString().padStart(2, "0");
  return `${m}:${s}`;
}

audio.play();
```

---

# 🟣 4. Ajouter une animation “boucle” comme Spotify Canvas

Spotify utilise des **vidéos courtes en boucle (3–8 secondes)**.  
Tu peux faire pareil :

- soit une **vidéo muted loop** derrière la pochette
- soit une **animation CSS** (gradient animé, particules, etc.)

Exemple rapide :

```html
<video class="canvas" autoplay loop muted>
  <source src="loop.mp4" type="video/mp4">
</video>
```

```css
.canvas {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.15;
  pointer-events: none;
}
```

---

# 🟡 5. Si tu veux, je peux te faire :
- une version **ultra clean** façon Spotify Premium  
- une version **minimaliste**  
- une version **avec particules animées** (tu adores ça 😉)  
- une version **intégrée directement dans ton projet CStream**  
- ou même un **overlay flottant** comme sur mobile

Tu veux quel style pour ton overlay ?